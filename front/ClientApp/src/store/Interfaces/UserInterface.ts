export interface User{
  firstName: string | null,
  lastName: string| null,
  city: string | null,
  number: number | null,
  country: string | null,
  userName: string | null,
  email:string|null
}
